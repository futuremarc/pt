extends ../landing/layout

block css

block content

  form(id='pt-follow-form', class='pt-form')

    div(class='error-message')
      h3 &nbsp;
    br
    div(id='auth-name-parent')
      input(class='input-lg auth-name form-control', name='name', placeholder='Enter friend\'s name', autocomplete='off')
    br
   
    input(type='submit', disabled="true", class='btn btn-lg btn-block btn-custom', value='Send request')

block javascript
  script(src='/js/local/auth/follow.js')